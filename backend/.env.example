# ── Google Maps Platform ──────────────────────────────────────────────────────
# Enable: Geocoding API, Distance Matrix API, Directions API
# Console: https://console.cloud.google.com/apis/credentials
GOOGLE_MAPS_API_KEY=your_google_maps_key_here
# Enable Places API (New) when regenerating lifestyle.json (see scripts/generate_lifestyle.py).

# ── Lifestyle Scoring (offline generation; app reads app/data/lifestyle.json) ──
# Walk Score: https://www.walkscore.com/professional/walk-score-apis.php
WALKSCORE_API_KEY=your_walkscore_key_here
# Optional tuning for scripts/generate_lifestyle.py
# LIFESTYLE_SEARCH_RADIUS_M=1000
# LIFESTYLE_GENERATE_DELAY_S=0.5

# ── OpenAI ────────────────────────────────────────────────────────────────────
# Powers AIExplanationService (GPT-4o or gpt-4o-mini recommended)
# Console: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_key_here
OPENAI_MODEL=gpt-4o-mini

# ── Housing Data (RentCast) ───────────────────────────────────────────────────
# Official API with a free tier (50 requests/month). Used by the
# RentCastListingProvider implementation of ListingProvider.
# Console: https://app.rentcast.io/app/api  →  generate an API key
RENTCAST_API_KEY=your_rentcast_key_here
# 'static' (default, no API calls) | 'rentcast' (live data)
LISTING_PROVIDER=static
# Hard ceiling on RentCast HTTP calls per process lifetime (free tier = 50/month)
RENTCAST_MAX_REQUESTS=40

# ── Mapbox (used by frontend; backend reads it to avoid embedding in JS bundle)
# Console: https://account.mapbox.com/access-tokens/
MAPBOX_TOKEN=your_mapbox_token_here

# ── PostgreSQL ────────────────────────────────────────────────────────────────
DATABASE_URL=postgresql://user:password@localhost:5432/relocateiq
JWT_SECRET=replace_with_a_long_random_string
